extends ./layout/defaultLayout

block content 
  include ./components/postSection
  +postSection({ PostButton: true, Type: 'Profile', Pages, CurrentPage }, Posts, Credentials, Author, Category)
  include ./components/sideBar
  +sideBar(Categories, Authors)    